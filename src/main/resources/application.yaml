spring:
  application:
    name: codesync-backend

  datasource:
    url: jdbc:mysql://localhost:3306/codesync
    username: root
    password: your_password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080